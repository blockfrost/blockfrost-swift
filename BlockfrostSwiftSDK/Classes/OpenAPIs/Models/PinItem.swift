//
// InlineResponse2005.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct PinItem: Codable, Hashable {

    public enum State: String, Codable, CaseIterable {
        case queued = "queued"
        case pinned = "pinned"
        case unpinned = "unpinned"
        case failed = "failed"
        case gc = "gc"
    }
    /** Creation time of the IPFS object on our backends */
    public var timeCreated: Int
    /** Pin time of the IPFS object on our backends */
    public var timePinned: Int
    /** IPFS hash of the pinned object */
    public var ipfsHash: String
    /** Size of the object in Bytes */
    public var size: String
    /** State of the pinned object, which is `queued` when we are retriving object. If this is successful the state is changed to `pinned` or `failed` if not. The state `gc` means the pinned item has been garbage collected due to account being over storage quota or after it has been moved to `unpinned` state by removing the object pin.  */
    public var state: State

    public init(timeCreated: Int, timePinned: Int, ipfsHash: String, size: String, state: State) {
        self.timeCreated = timeCreated
        self.timePinned = timePinned
        self.ipfsHash = ipfsHash
        self.size = size
        self.state = state
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case timeCreated = "time_created"
        case timePinned = "time_pinned"
        case ipfsHash = "ipfs_hash"
        case size
        case state
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(timeCreated, forKey: .timeCreated)
        try container.encode(timePinned, forKey: .timePinned)
        try container.encode(ipfsHash, forKey: .ipfsHash)
        try container.encode(size, forKey: .size)
        try container.encode(state, forKey: .state)
    }
}

