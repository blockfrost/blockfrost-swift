//
// TxContentMir.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
    import AnyCodable
#endif

public final class TxContentMir: Codable, Hashable {
    public enum Pot: String, Codable, CaseIterable {
        case reserve
        case treasury
    }

    /** Source of MIR funds */
    public var pot: Pot
    /** Index of the certificate within the transaction */
    public var certIndex: Int
    /** Bech32 stake address */
    public var address: String
    /** MIR amount in Lovelaces */
    public var amount: String

    public init(pot: Pot, certIndex: Int, address: String, amount: String) {
        self.pot = pot
        self.certIndex = certIndex
        self.address = address
        self.amount = amount
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case pot
        case certIndex = "cert_index"
        case address
        case amount
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(pot, forKey: .pot)
        try container.encode(certIndex, forKey: .certIndex)
        try container.encode(address, forKey: .address)
        try container.encode(amount, forKey: .amount)
    }

    public static func == (lhs: TxContentMir, rhs: TxContentMir) -> Bool {
        lhs.pot == rhs.pot &&
            lhs.certIndex == rhs.certIndex &&
            lhs.address == rhs.address &&
            lhs.amount == rhs.amount
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(pot.hashValue)
        hasher.combine(certIndex.hashValue)
        hasher.combine(address.hashValue)
        hasher.combine(amount.hashValue)
    }
}
