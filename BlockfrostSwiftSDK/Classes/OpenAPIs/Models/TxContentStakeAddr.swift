//
// TxContentStakeAddr.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
    import AnyCodable
#endif

public final class TxContentStakeAddr: Codable, Hashable {
    /** Index of the certificate within the transaction */
    public var certIndex: Int
    /** Delegation stake address */
    public var address: String
    /** Registration boolean, false if deregistration */
    public var registration: Bool

    public init(certIndex: Int, address: String, registration: Bool) {
        self.certIndex = certIndex
        self.address = address
        self.registration = registration
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case certIndex = "cert_index"
        case address
        case registration
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(certIndex, forKey: .certIndex)
        try container.encode(address, forKey: .address)
        try container.encode(registration, forKey: .registration)
    }

    public static func == (lhs: TxContentStakeAddr, rhs: TxContentStakeAddr) -> Bool {
        lhs.certIndex == rhs.certIndex &&
            lhs.address == rhs.address &&
            lhs.registration == rhs.registration
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(certIndex.hashValue)
        hasher.combine(address.hashValue)
        hasher.combine(registration.hashValue)
    }
}
